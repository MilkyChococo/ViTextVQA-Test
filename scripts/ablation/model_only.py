from __future__ import annotations


MODE = {
    "name": "model_only",
    "description": "Vanilla VLM: original image + question only.",
    "requires_graph": False,
    "requires_retrieval_models": False,
    "strategy": "image_only",
}
